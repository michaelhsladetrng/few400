import { Environment } from './models';

export const environment: Environment = {
  production: true,
  authUrl: 'some production url',
  todosUrl: 'some production url',
  todosWsUrl: 'some production websocket url'
};
